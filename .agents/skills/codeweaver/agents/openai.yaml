interface:
  display_name: "Codeweaver"
  short_description: "Repo-specific patterns and workflows for codeweaver"
  default_prompt: "Use the codeweaver repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true