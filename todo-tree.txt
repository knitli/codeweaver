└─ codeweaver-mcp
   ├─ scripts
   │  ├─ dev-env
   │  │  └─ install-mise.sh
   │  │     ├─ line 157: TODO : refactor this, it's a bit messy
   │  │     └─ line 228: TODO : verify with minisign or gpg if available
   │  └─ cwdev.py
   │     └─ line 11: TODO : The plan for this script is to make it an overall dev helper to do things like:
   ├─ src
   │  ├─ agent_api
   │  │  └─ find_code.py
   │  │     └─ line 74: TODO : WHY IS THIS COMMENTED OUT? ... here's why:
   │  ├─ common
   │  │  ├─ utils
   │  │  │  └─ normalize.py
   │  │  │     ├─ line 26: TODO : Add Rebuff.ai integration, and/or other advanced sanitization options. Probably as middleware.
   │  │  │     └─ line 52: TODO : Need to add a mechanism to override or customize the injection patterns.
   │  │  ├─ registry.py
   │  │  │  ├─ line 96: TODO : We need to integrate feature detection into service registration and validation
   │  │  │  ├─ line 312: TODO register default services
   │  │  │  └─ line 1481: TODO : Services registry... we'll need to deconflict available providers, capabilities, against registered services to figure out what's actually available
   │  │  └─ statistics.py
   │  │     └─ line 594: TODO : We'd ideally want to make sure these are pushed to the indexer, unless we receive these in the same action
   │  ├─ config
   │  │  ├─ logging.py
   │  │  │  └─ line 194: TODO : Once they become more available, we should use `raw template strings` here
   │  │  ├─ settings.py
   │  │  │  └─ line 638: TODO : I don't think we're actually checking for these before initializing the server. We should.
   │  │  └─ types.py
   │  │     └─ line 280: TODO : We should add it, but we need to manage handling it mid-request.
   │  ├─ core
   │  │  ├─ types
   │  │  │  └─ enum.py
   │  │  │     └─ line 39: TODO : For our larger enums with lots of property methods, like `SemanticSearchLanguage`, it probably makes sense to define it as an enum with type `dataclass` members, where most of the properties are defined on the dataclass, and the enum just holds instances of that dataclass. This would make it easier to manage and extend the enum members. See https://docs.python.org/3/howto/enum.html#dataclass-support. This would also make it easier to add new members without having to add the member to the enum methods themselves. Since attributes and properties are accessed the same way, we could do this without it being a breaking change. Classes that would benefit from this treatment:
   │  │  ├─ chunks.py
   │  │  │  ├─ line 188: TODO : Add method to retrieve embedding information (model capabilities, dimensions and other settings)
   │  │  │  └─ line 312: TODO : Replace with proper tokenizer (tiktoken, transformers).
   │  │  ├─ discovery.py
   │  │  │  └─ line 113: TODO : A better approach for files that we can semantically analyze is to hash the AST or structure instead of the raw file contents and compare those.
   │  │  ├─ language.py
   │  │  │  ├─ line 364: TODO : Validate the `dependency_key_paths` for each config file to ensure they are correct. If you use these languages, please let us know if you find any issues with the `dependency_key_paths` in the config files. Some are probably incorrect.
   │  │  │  └─ line 1494: TODO : Integrate into indexing and search services to use these languages.
   │  │  └─ metadata.py
   │  │     └─ line 91: TODO : Logic for symbol extraction from AST nodes
   │  ├─ engine
   │  │  ├─ chunker
   │  │  │  ├─ delimiter.py
   │  │  │  │  └─ line 1041: TODO : Implement proper conversion when delimiter families are integrated
   │  │  │  ├─ registry.py
   │  │  │  │  └─ line 83: TODO : We need to send a signal to vector stores to remove associated vectors
   │  │  │  ├─ selector.py
   │  │  │  │  ├─ line 104: TODO : Implement DelimiterChunker for proper fallback
   │  │  │  │  ├─ line 135: TODO : Consider returning a NullChunker or raising FileTooLargeError
   │  │  │  │  ├─ line 206: TODO : Implement proper extension-to-language mapping when needed
   │  │  │  │  ├─ line 229: TODO 
   │  │  │  │  └─ line 232: TODO 
   │  │  │  └─ semantic.py
   │  │  │     ├─ line 123: TODO : Make configurable via governor
   │  │  │     ├─ line 198: TODO : Implement proper token estimation
   │  │  │     └─ line 595: TODO : Implement proper token estimation
   │  │  ├─ indexer.py
   │  │  │  └─ line 11: TODO : register with providers registry
   │  │  └─ textify.py
   │  │     └─ line 8: TODO : Once we have better code parsing implementations, we need to define a pipeline for processing.
   │  ├─ providers
   │  │  ├─ embedding
   │  │  │  └─ mistral.py
   │  │  │     └─ line 31: TODO : Add support for batch jobs. Ideal for most usage, but becomes more difficult to manage state.
   │  │  ├─ reranking
   │  │  │  ├─ capabilities
   │  │  │  │  └─ base.py
   │  │  │  │     └─ line 190: TODO : We need to handle the case where the first chunk is larger than the max_input.
   │  │  │  └─ __init__.py
   │  │  │     └─ line 14: TODO : Implement the same system we have for Embedding models
   │  │  ├─ vector_stores
   │  │  │  └─ inmemory.py
   │  │  │     └─ line 260: TODO : Extract metadata from payload if needed
   │  │  ├─ capabilities.py
   │  │  │  └─ line 58: TODO : The vector provider capabilities aren't what they need to be.... it needs to be things like sparse vectors, quantization, etc.
   │  │  └─ provider.py
   │  │     └─ line 110: TODO : We need to allow for dynamic providers in the future, we would check if there's a provider class registered for the value, then register the provider here with `cls.add_member("NEW_PROVIDER", "new_provider")`.
   │  ├─ semantic
   │  │  └─ line 17: TODO : We got into a bad habit of not using native exception types. We need to systematically go through and improve exception handling and information.
   │  └─ server.py
   │     ├─ line 141: TODO : Expand to be more dynamic, computing health based on service status, etc.
   │     ├─ line 285: TODO : These can all be boolean but need to differentiate from defaults vs set values
   │     └─ line 364: TODO : Add state caching/saving and cleanup logic here
   └─ tests
      └─ test_selector.py
         └─ line 41: TODO : Change to DelimiterChunker when implementation is complete
