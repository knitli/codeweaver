{
  "dependencies": {
    "@astrojs/starlight": "^0.38.3",
    "@knitli/astro-docs-template": "^0.5.3",
    "@knitli/docs-components": "^1.4.6"
  },
  "devDependencies": {
    "@astrojs/check": "latest",
    "@knitli/tsconfig": "^0.3.0",
    "@types/node": "^25.6.0",
    "typescript": "latest",
    "wrangler": "latest"
  },
  "name": "@knitli/codeweaver-docs",
  "private": true,
  "scripts": {
    "astro": "bunx astro",
    "build": "bunx astro build",
    "clean": "rm -rf dist",
    "deploy": "bun run build && bunx wrangler deploy -c ./wrangler.jsonc",
    "dev": "bunx astro dev",
    "postinstall": "node scripts/patch-astro-prerender.mjs",
    "prebuild": "bun run clean && bunx wrangler types; bunx astro sync",
    "prepare": "test -f ./node_modules/bun/install.js && node ./node_modules/bun/install.js || true",
    "preview": "bunx astro preview",
    "wrangler": "bunx wrangler"
  },
  "trustedDependencies": [
    "bun"
  ],
  "type": "module",
  "version": "0.0.1"
}