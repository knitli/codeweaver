---
title: generation
description: API reference for codeweaver.core.utils.generation
---

# `codeweaver.core.utils.generation`

Utility functions for generating identifiers and hashes.

## Function: `get_blake_hash`

```python
get_blake_hash()
```

Hash a value using blake3 and return the hex digest.

## Function: `get_blake_hash_generic`

```python
get_blake_hash_generic()
```

Hash a value using blake3 and return the hex digest - generic version.

## Function: `uuid7`

```python
uuid7()
```

Generate a new UUID7.

## Function: `uuid7_as_timestamp`

```python
uuid7_as_timestamp()
```

Utility to extract the timestamp from a UUID7, optionally as a datetime.
