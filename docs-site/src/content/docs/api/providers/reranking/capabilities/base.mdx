---
title: base
description: API reference for codeweaver.providers.reranking.capabilities.base
---

# `codeweaver.providers.reranking.capabilities.base`

Reranking model capabilities and settings.

## Class: `RerankingModelCapabilities`

Capabilities of a reranking model.

### Method: `is_within_limits`

```python
is_within_limits()
```

Check if the input chunks are within the model's limits.

### Method: `query_ok`

```python
query_ok()
```

Check if the query is within the model's limits.
