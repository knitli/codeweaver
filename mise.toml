# SPDX-FileCopyrightText: 2025 Knitli Inc.
# SPDX-FileContributor: Adam Poulemanos <adam@knit.li>
#
# SPDX-License-Identifier: MIT OR Apache-2.0
[settings]
idiomatic_version_file_enable_tools = []

[settings.python]
uv_venv_auto = true
compile = true
uv_venv_create_args = [
  "--seed",
  "--keyring-provider",
  "subprocess",
  "--python",
  "3.13",
]

[tools]
python = "3.13"
gh = "latest"
hk = "latest"
pkl = "latest"
uv = "latest"

[env]
GH_REPO = "knitli/codeweaver"
REMOTE_ORIGIN_URL = '''http://github.com/knitli/codeweaver.git'''
# Use the project name derived from the current directory
PROJECT_NAME = "{{ config_root | basename }}"
CODEWEAVER_PROJECT_PATH = '''{{ config_root }}'''
# Get version from git tags (uv-dynamic-versioning uses git as source)
# Falls back to "dev" if git command fails
CODEWEAVER_VERSION = '''{{ exec(command='git describe --tags --always --dirty 2>/dev/null || echo "dev"') | trim }}'''
CI = '''{% if get_env(name="GITHUB_ACTIONS", default="") != "" %}true{% else %}false{% endif %}'''
_.path = [
  "scripts/dev-env",
  "scripts/code-quality",
  "scripts/build",
  "scripts/language-support",
  "scripts/testing",
  "scripts/utils",
  ".venv/bin",
]
MISE_ENV = "dev"

[tasks.info]
description = "Print project information"
run = '''
echo "Project: $PROJECT_NAME"
echo "Virtual Environment: $VIRTUAL_ENV"
'''
run_windows = '''
echo Project: %PROJECT_NAME%
echo Virtual Environment: %VIRTUAL_ENV%
'''

[tasks.sync]
depends = ["source"]
tools.uv = "latest"
run = '''
echo "Syncing dependencies..."
uv sync --extra full --group dev --group docs --group build --group test
'''
run_windows = '''
echo Syncing dependencies...
uv sync --extra full --group dev --group docs --group build --group test
'''

[tasks.source]
run = '''
#!/usr/bin/env zsh
# Source the virtual environment
print -P "%F{209}[codeweaver]%f Sourcing virtual environment..."
git rev-parse --show-toplevel || true
source .venv/bin/activate
'''
run_windows = '''
echo [codeweaver] Sourcing virtual environment...
git rev-parse --show-toplevel
.venv\Scripts\activate.bat
'''

[tasks.setup]
# Development setup
shell = "zsh -c"
run = [
  "print -P \"%F{209}[codeweaver]%f Setting up development environment for the first time...\"",
  "mise set &>/dev/null",
  "mise -y trust &>/dev/null",
  "mise -y install &>/dev/null || true",
  "mise -y venv &>/dev/null",
  "mise -y run sync &>/dev/null",
  "mise env &>/dev/null || true",
  "git config include.path .gitconfig &>/dev/null || true",
  "echo 'src/codeweaver/_version.py' >> .git/info/exclude",
  "echo 'coverage.xml' >> .git/info/exclude",
  "ln -sf AGENTS.md CLAUDE.md",
  "eval \"$(mise activate zsh)\" &>/dev/null",
]
run_windows = [
  "echo [codeweaver] Setting up development environment for the first time...",
  "mise set > NUL 2>&1",
  "mise -y trust > NUL 2>&1",
  "mise -y install > NUL 2>&1 || echo.",
  "mise -y venv > NUL 2>&1",
  "mise run sync > NUL 2>&1",
  "mise env > NUL 2>&1",
  "git config include.path .gitconfig > NUL 2>&1",
  "echo src/codeweaver/_version.py >> .git\\info\\exclude",
  "echo coverage.xml >> .git\\info\\exclude",
  "mklink CLAUDE.md AGENTS.md > NUL 2>&1",
  "mise activate cmd",
]
silent = "stdout"

[tasks.fix-python]
usage = '''
arg "[dir]" help="directories to fix" var=#true default="."
'''
run = [
  "uvx ruff format \"${usage_dir}\"",
  "uvx ruff check --fix --unsafe-fixes \"${usage_dir}\"",
]
run_windows = [
  "uvx ruff format .",
  "uvx ruff check --fix --unsafe-fixes .",
]
file = "scripts/code-quality/fix-ruff-patterns.sh"
sources = ["{src,scripts,tests}/*.py"]

[tasks.check]
tools.hk = "latest"
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Running quality checks..."
hk check
'''
run_windows = '''
echo [codeweaver] Running quality checks...
hk check
'''

[tasks.lint]
tools.uv = "latest"
run = '''
#!/bin/bash
echo "Running linting checks..."
if [[ "$CI" == "true" ]]; then
    uv run ruff check . --output-format=github
else
    uv run ruff check .
fi
'''
run_windows = '''
echo Running linting checks...
if "%CI%"=="true" (
    uv run ruff check . --output-format=github
) else (
    uv run ruff check .
)
'''

[tasks.format]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Running formatting checks..."
hk fix
'''
run_windows = '''
echo [codeweaver] Running formatting checks...
hk fix
'''

[tasks.format-fix]
tools."cargo:rust-parallel" = "latest"
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Applying formatting...hold onto your butts 🚬...🦖 "
# mise run fix-ruff-patterns || {  # there are some bugs to work out
#    print -P "%F{209}[codeweaver]%f ⚠️  %F{red}Ruff pattern fixer encountered issues.%f"}
uv run ruff format .
files="$(git ls-files "*.yaml" "*.yml" "*.json" 2>/dev/null)"
if [ -n "$files" ]; then
    echo "$files" | rust-parallel -d stderr -i - --no-run-if-empty yq -iP --indent=2 "." || true
fi
files="$(git ls-files "*.toml" 2>/dev/null)"
if [ -n "$files" ]; then
    tombi format $files 2>/dev/null || true
fi
'''
run_windows = '''
echo [codeweaver] Applying formatting...hold onto your butts 🚬...🦖
uv run ruff format .
for /f "delims=" %%f in ('git ls-files "*.yaml" "*.yml" "*.json" 2^>NUL') do (
    yq -iP --indent=2 "." "%%f" 2>NUL
)
for /f "delims=" %%f in ('git ls-files "*.toml" 2^>NUL') do (
    tombi format "%%f" 2>NUL
)
'''

[tasks.graph]
usage = '''
arg "[dir]" help="python directory to generate a graph for" default="src"
'''
run = '''mise x -- ruff analyze graph --target-version py312 --python .venv/bin/python "${usage_dir:-src/}"'''
run_windows = '''mise x -- ruff analyze graph --target-version py312 --python .venv\Scripts\python.exe .'''

[tasks.tree]
run = '''uv tree --no-dev --group full'''

[tasks.venv]
shell = "zsh -c"
run = '''
print -P "%F{209}[codeweaver]%f Setting up Python virtual environment..."
uv venv --allow-existing --seed --keyring-provider subprocess --python 3.13 --refresh .venv &&
print -P "%F{209}[codeweaver]%f ✅ Virtual environment is ready!"
source .venv/bin/activate || print -P "%F{209}[codeweaver]%f ⚠️  %F{red}Failed to source virtual environment.%f"
print -P "%F{209}[codeweaver]%f Running dependency sync..."
mise run sync &
'''
run_windows = '''
echo [codeweaver] Setting up Python virtual environment...
uv venv --allow-existing --seed --keyring-provider subprocess --python 3.13 --refresh .venv
if %ERRORLEVEL% equ 0 (
    echo [codeweaver] Virtual environment is ready!
    call .venv\Scripts\activate.bat
    if %ERRORLEVEL% neq 0 (
        echo [codeweaver] WARNING: Failed to source virtual environment.
    )
)
echo [codeweaver] Running dependency sync...
start /b mise run sync
'''

[tasks.test]
# Testing
usage = '''
args "[dir]" help="directory to test" default="tests/"
flag "--marks [markers]" help="pytest markers to filter tests" default=""

'''
run = '''
#!/bin/bash
echo "Running tests..."
uv run pytest
'''
run_windows = '''
echo Running tests...
uv run pytest
'''

[tasks.test-cov]
run = '''
#!/bin/bash
echo "Running tests with coverage..."
uv run pytest tests/ --cov=codeweaver --cov-report=xml --cov-report=term-missing --junit-xml=test-results.xml -v "$@"
'''
run_windows = '''
echo Running tests with coverage...
uv run pytest tests/ --cov=codeweaver --cov-report=xml --cov-report=term-missing --junit-xml=test-results.xml -v %*
'''

[tasks.clean]
run = '''
#!/bin/bash
echo "Cleaning build artifacts..."
rm -rf dist/ build/ *.egg-info/ .coverage test-results.xml &&
find . -type d -name __pycache__ -exec rm -rf {} + 2>/dev/null || true &&
find . -type f -name "*.pyc" -delete &&
echo "✅ Build artifacts cleaned!"
'''
run_windows = '''
echo Cleaning build artifacts...
if exist dist rd /s /q dist
if exist build rd /s /q build
if exist *.egg-info rd /s /q *.egg-info
if exist .coverage del .coverage
if exist test-results.xml del test-results.xml
for /d /r . %%d in (__pycache__) do @if exist "%%d" rd /s /q "%%d"
del /s /q *.pyc 2>NUL
echo Build artifacts cleaned!
'''

# Release and maintenance
[tasks.version]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver] CodeWeaver%f version information:"
print -P "%F{209}[codeweaver]%f 📦 Package version: $(uv run python -c "import tomllib; print(tomllib.load(open('pyproject.toml', 'rb'))['project']['version'])")"
print -P "%F{209}[codeweaver]%f 🐍 Python version: $(python --version)"
print -P "%F{209}[codeweaver]%f 🌞 UV version: $(uv --version)"
'''
run_windows = '''
echo [codeweaver] CodeWeaver version information:
for /f "delims=" %%v in ('uv run python -c "import tomllib; print(tomllib.load(open('pyproject.toml', 'rb'))['project']['version'])"') do echo [codeweaver] Package version: %%v
for /f "delims=" %%v in ('python --version') do echo [codeweaver] Python version: %%v
for /f "delims=" %%v in ('uv --version') do echo [codeweaver] UV version: %%v
'''

[tasks.pre-commit]
shell = "zsh -c"
run = [
  "print -P \"%F{209}[codeweaver]%f Running %F{209}CodeWeaver%f pre-commit checks 🪝... %F{38} Thanks for your contributions! You're pretty cool.%f 🚀\" || true",
  "hk fix || print -P \"%F{209}[codeweaver]%f ⚠️  %F{red}You need to fix some issues%f before committing. %F{209}Do not pass go, do not collect 200 dollars.%f 💸 😦\" && exit 1",
  "git add .",
  "git commit --amend --no-edit || true",
  "hk check || print -P \"%F{209}[codeweaver]%f ⚠️  %F{red}You need to fix some issues before committing. Almost there!%f\" && exit 1",
  "print -P \"%F{209}[codeweaver]%f ✅ %F{green}All pre-commit checks passed! You're awesome!%f 🎉 ... and they said the Kobiyashi Maru was unwinnable! 🖖\"",
]
run_windows = '''
echo [codeweaver] Running CodeWeaver pre-commit checks... Thanks for your contributions! You're pretty cool. 🚀
hk fix
if %ERRORLEVEL% neq 0 (
    echo [codeweaver] WARNING: You need to fix some issues before committing.
    exit /b 1
)
git add .
hk check
if %ERRORLEVEL% neq 0 (
    echo [codeweaver] WARNING: You need to fix some issues before committing. Almost there!
    exit /b 1
)
git add .
echo [codeweaver] All pre-commit checks passed! You're awesome!
'''

#! SECTION Aliases/Shortcuts for Scripts in scripts/ directory
[tasks.fix-ruff-patterns]
alias = "ruff-pats"
description = "Fix ruff unfixable patterns in the codebase"
run = '''
#!/bin/bash
echo "Running ruff pattern fixer..."
./scripts/code-quality/fix-ruff-patterns.sh "${*}"
'''
run_windows = '''
echo Running ruff pattern fixer...
bash scripts/code-quality/fix-ruff-patterns.sh %*
'''

[tasks.update-licenses]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Updating %F{209}CodeWeaver%f license headers :..."
uv run -s scripts/code-quality/update-licenses.py --interactive "${*}"
'''
run_windows = '''
echo [codeweaver] Updating CodeWeaver license headers...
uv run -s scripts/code-quality/update-licenses.py --interactive %*
'''

[tasks.dev-shell-init]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Initializing development shell..."
./scripts/dev-env/dev-shell-init.zsh
'''

[tasks.install-mise]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Installing Mise..."
./scripts/dev-env/install-mise.sh
'''

[tasks.ci-free-disk-space]
description = "Free up disk space on CI runners by removing unused packages"
run = '''
#!/usr/bin/env bash
./scripts/dev-env/ci-free-disk-space.sh
'''

[tasks.vscode-terminal-bootstrap]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Bootstrapping VSCode terminal..."
./scripts/dev-env/vscode-terminal-bootstrap.sh
'''

[tasks.add-plaintext-to-codeblock]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Adding 'plaintext' to unmarked code blocks in markdown files..."
uv run -s scripts/docs/add-plaintext-to-codeblock.py
'''
run_windows = '''
echo [codeweaver] Adding 'plaintext' to unmarked code blocks in markdown files...
uv run -s scripts/docs/add-plaintext-to-codeblock.py
'''

[tasks.gen-ref-pages]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Generating docs reference pages..."
uv run -s scripts/docs/gen-ref-pages.py
'''
run_windows = '''
echo [codeweaver] Generating docs reference pages...
uv run -s scripts/docs/gen-ref-pages.py
'''

[tasks.analyze-grammar-structure]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Analyzing grammar structure..."
uv run -s scripts/language-support/analyze-grammar-structure.py
'''
run_windows = '''
echo [codeweaver] Analyzing grammar structure...
uv run -s scripts/language-support/analyze-grammar-structure.py
'''

[tasks.build-language-mappings]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Building language mappings..."
uv run -s scripts/language-support/build-language-mappings.py
'''
run_windows = '''
echo [codeweaver] Building language mappings...
uv run -s scripts/language-support/build-language-mappings.py
'''

[tasks.compare-delimiters]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Comparing delimiters..."
uv run -s scripts/language-support/compare-delimiters.py
'''
run_windows = '''
echo [codeweaver] Comparing delimiters...
uv run -s scripts/language-support/compare-delimiters.py
'''

[tasks.download-ts-grammars]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Downloading tree-sitter grammars..."
uv run -s scripts/language-support/download-ts-grammars.py "${*}"
'''
run_windows = '''
echo [codeweaver] Downloading tree-sitter grammars...
uv run -s scripts/language-support/download-ts-grammars.py %*
'''

[tasks.generate-supported-languages]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Generating supported languages..."
uv run -s scripts/build/generate-supported-languages.py
'''
run_windows = '''
echo [codeweaver] Generating supported languages...
uv run -s scripts/build/generate-supported-languages.py
'''

[tasks.mteb-to-codeweaver]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Converting MTEB model data to CodeWeaver format..."
uv run -s scripts/model-data/mteb-to-codeweaver.py "${*}"
'''
run_windows = '''
echo [codeweaver] Converting MTEB model data to CodeWeaver format...
uv run -s scripts/model-data/mteb-to-codeweaver.py %*
'''

[tasks.apply-test-marks]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Applying test marks to tests..."
uv run -s scripts/testing/apply-test-marks.py "${*}"
'''
run_windows = '''
echo [codeweaver] Applying test marks to tests...
uv run -s scripts/testing/apply-test-marks.py %*
'''

[tasks.benchmark-detection]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Running benchmark detection..."
uv run -s scripts/testing/benchmark-detection.py "${*}"
'''
run_windows = '''
echo [codeweaver] Running benchmark detection...
uv run -s scripts/testing/benchmark-detection.py %*
'''

[tasks.ansi-color-tests]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Checking ANSI color codes..."
uv run -s scripts/utils/ansi-color-tests.py "${*}"
'''
run_windows = '''
echo [codeweaver] Checking ANSI color codes...
uv run -s scripts/utils/ansi-color-tests.py %*
'''

[tasks.check-imports]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Checking imports..."
uv run -s scripts/utils/check-imports.py "${*}"
'''
run_windows = '''
echo [codeweaver] Checking imports...
uv run -s scripts/utils/check-imports.py %*
'''

[tasks.get-all-exceptions]
run = '''
#!/usr/bin/env zsh
print -P "%F{209}[codeweaver]%f Getting all exceptions..."
uv run -s scripts/utils/get-all-exceptions.py "${*}"
'''
run_windows = '''
echo [codeweaver] Getting all exceptions...
uv run -s scripts/utils/get-all-exceptions.py %*
'''

[tasks.activate]
run = '''eval "$(mise activate zsh)" &>/dev/null'''
run_windows = '''mise activate cmd'''
hide = true
